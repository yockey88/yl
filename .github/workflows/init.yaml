name: Build Project

on: 
  push:
    branches:
      - master
  pull_request:
    branches:
      - master 
      - dev

jobs:
  build-project: 
    runs-on: "windows-latest"
    steps:
      - uses: actions/checkout@v4
